/* PARLAY TRACKER SOURCES V53 — ESPN primary, MLB supplemental, refresh-scoped caches */
(() => {
  'use strict';
  const C=window.ParlayTrackerCore;
  let summaryCache=new Map(),mlbScheduleCache=new Map(),mlbFeedCache=new Map();
  function resetTrackingCaches(){summaryCache=new Map();mlbScheduleCache=new Map();mlbFeedCache=new Map()}
  async function jsonFetch(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);return r.json()}
  function cacheRequest(map,key,loader){if(map.has(key))return map.get(key);const p=Promise.resolve().then(loader).then(v=>{if(v==null)map.delete(key);return v}).catch(e=>{map.delete(key);throw e});map.set(key,p);return p}
  function sportPath(sport){return sport==='nba'?'basketball/nba':sport==='wnba'?'basketball/wnba':'baseball/mlb'}
  async function fetchScoreboards(dates){let events=[];for(const date of [...new Set(dates.filter(Boolean))]){for(const sport of ['mlb','nba','wnba']){try{const data=await jsonFetch(`https://site.api.espn.com/apis/site/v2/sports/${sportPath(sport)}/scoreboard?dates=${date}&limit=100`);for(const e of data.events||[])e.__sport=sport;events=events.concat(data.events||[])}catch{}}}return events}
  async function fetchSummary(event){if(!event?.id)return null;return cacheRequest(summaryCache,event.id,()=>jsonFetch(`https://site.api.espn.com/apis/site/v2/sports/${sportPath(event.__sport||'mlb')}/summary?event=${event.id}`).catch(()=>null))}
  function mlbDateParam(v){const s=String(v||'');return `${s.slice(0,4)}-${s.slice(4,6)}-${s.slice(6,8)}`}
  async function fetchMLBSchedule(date){const key=mlbDateParam(date);return cacheRequest(mlbScheduleCache,key,()=>jsonFetch(`https://statsapi.mlb.com/api/v1/schedule?sportId=1&date=${key}`).then(d=>(d.dates||[]).flatMap(x=>x.games||[]))) }
  function mlbTeamMatches(team,abbr){const names=C.TEAM_ALIASES[abbr]||[abbr];const vals=[team?.abbreviation,team?.teamName,team?.name,team?.shortName,team?.clubName,team?.locationName].filter(Boolean);return vals.some(v=>names.some(n=>C.clean(v)===C.clean(n)))}
  function scheduleStartCT(game){return C.gameStartCT({date:game?.gameDate})}
  async function resolveMLBGamePk(date,key,event,ticket,leg){const [away,home]=String(key||'').split('@');let games=(await fetchMLBSchedule(date)).filter(g=>mlbTeamMatches(g.teams?.away?.team,away)&&mlbTeamMatches(g.teams?.home?.team,home));if(!games.length)return null;games=[...games].sort((a,b)=>new Date(a?.gameDate||0)-new Date(b?.gameDate||0));const explicitPk=Number(C.instanceValue(ticket,leg,'gamePk'));if(Number.isFinite(explicitPk)){const exact=games.find(g=>Number(g.gamePk)===explicitPk);if(exact)return exact.gamePk}const eventStart=C.gameStartCT(event);const wantedStart=String(C.instanceValue(ticket,leg,'gameStart')||eventStart||'').trim();if(wantedStart){const exact=games.find(g=>scheduleStartCT(g)===wantedStart);if(exact)return exact.gamePk}const ordinal=Number(C.instanceValue(ticket,leg,'gameNumber'));if(Number.isInteger(ordinal)&&ordinal>=1&&ordinal<=games.length)return games[ordinal-1].gamePk;const rawRef=event?.date||C.instanceValue(ticket,leg,'gameSavedAt')||ticket?.__recordReferenceTime||'';const ref=new Date(rawRef).getTime();if(Number.isFinite(ref)){const nearest=games.reduce((best,g)=>{const t=new Date(g?.gameDate||'').getTime();if(!Number.isFinite(t))return best;const distance=Math.abs(t-ref);return !best||distance<best.distance?{g,distance}:best},null);if(nearest)return nearest.g.gamePk}return games[0].gamePk}
  async function fetchMLBFeed(ticket,leg,event){const date=leg?.date||ticket?.date,key=C.legGame(ticket,leg);if(!date||!key)return null;const pk=await resolveMLBGamePk(date,key,event,ticket,leg);if(!pk)return null;return cacheRequest(mlbFeedCache,pk,()=>jsonFetch(`https://statsapi.mlb.com/api/v1.1/game/${pk}/feed/live`).catch(()=>null))}
  function playerStats(summary,teamAbbr,playerName){if(!summary?.boxscore)return null;const target=C.clean(playerName);const rowMatches=row=>{const a=row.athlete||{};const nm=C.clean(a.displayName||a.shortName||a.fullName||row.displayName||row.name);return nm&&(nm.includes(target)||target.includes(nm))};const read=container=>{let merged=null;for(const group of container.statistics||[]){const labels=group.labels||[],names=group.names||[];for(const row of group.athletes||[]){if(!rowMatches(row))continue;const stats=row.stats||[];if(!merged)merged={};labels.forEach((k,i)=>merged[k]=stats[i]);names.forEach((k,i)=>merged[k]=stats[i])}}return merged};for(const team of summary.boxscore.players||[]){if(C.teamMatches(team.team||{},teamAbbr)){const r=read(team);if(r)return r}}for(const team of summary.boxscore.teams||[]){if(C.teamMatches(team.team||{},teamAbbr)){const r=read(team);if(r)return r}}return null}
  function nStat(stats,keys){if(!stats)return null;for(const k of keys){if(stats[k]==null)continue;const raw=String(stats[k]);if(raw.includes('-'))continue;const n=Number(raw);if(!Number.isNaN(n))return n}return null}
  function maxKnown(...vals){const known=vals.filter(v=>v!=null&&!Number.isNaN(Number(v))).map(Number);return known.length?Math.max(...known):null}
  function getHits(s,t,p){return nStat(playerStats(s,t,p),['H','hits','Hits'])}
  function getHR(s,t,p){return nStat(playerStats(s,t,p),['HR','homeRuns','Home Runs'])}
  function getRuns(s,t,p){return nStat(playerStats(s,t,p),['R','runs','Runs'])}
  function getRBI(s,t,p){return nStat(playerStats(s,t,p),['RBI','runsBattedIn','Runs Batted In'])}
  function getWalks(s,t,p){return nStat(playerStats(s,t,p),['BB','walks','Walks'])}
  function getStolenBases(s,t,p){return nStat(playerStats(s,t,p),['SB','stolenBases','Stolen Bases'])}
  function getPoints(s,t,p){return nStat(playerStats(s,t,p),['PTS','points','Points'])}
  function getRebounds(s,t,p){return nStat(playerStats(s,t,p),['REB','rebounds','Rebounds'])}
  function getAssists(s,t,p){return nStat(playerStats(s,t,p),['AST','assists','Assists'])}
  function getBlocks(s,t,p){return nStat(playerStats(s,t,p),['BLK','blocks','Blocks'])}
  function getThrees(s,t,p){const st=playerStats(s,t,p);const direct=nStat(st,['3PM','3PTM','FG3M','threePointFieldGoalsMade']);if(direct!=null)return direct;for(const k of ['3PT','3PT FG','3-PT','3 Point']){const raw=String(st?.[k]??'');if(raw.includes('-')){const n=Number(raw.split('-')[0]);if(!Number.isNaN(n))return n}}return null}
  function getDoubleCount(s,t,p){return [getPoints(s,t,p)||0,getRebounds(s,t,p)||0,getAssists(s,t,p)||0].filter(v=>v>=10).length}
  function parseIPToOuts(raw){if(raw==null)return null;const s=String(raw).trim();if(!s)return null;if(s.includes('.')){const [i,f='0']=s.split('.').map(Number);return Number.isNaN(i)||Number.isNaN(f)?null:i*3+f}const n=Number(s);return Number.isNaN(n)?null:Math.round(n*3)}
  function getPitcherOuts(s,t,p){const st=playerStats(s,t,p);for(const k of ['IP','inningsPitched','Innings Pitched'])if(st?.[k]!=null)return parseIPToOuts(st[k]);return null}
  function mlbPlayer(feed,teamAbbr,playerName){const target=C.clean(playerName);for(const side of ['away','home']){const team=feed?.liveData?.boxscore?.teams?.[side];if(!mlbTeamMatches(team?.team,teamAbbr))continue;for(const p of Object.values(team.players||{})){const nm=C.clean(p?.person?.fullName||p?.person?.boxscoreName||'');if(nm&&(nm.includes(target)||target.includes(nm)))return p}}return null}
  function mlbBatting(feed,t,p){return mlbPlayer(feed,t,p)?.stats?.batting||null}
  function mlbPitching(feed,t,p){return mlbPlayer(feed,t,p)?.stats?.pitching||null}
  function getMLBHits(feed,t,p){const b=mlbBatting(feed,t,p);return b?Number(b.hits??0):null}
  function getMLBHR(feed,t,p){const b=mlbBatting(feed,t,p);return b?Number(b.homeRuns??0):null}
  function getMLBRuns(feed,t,p){const b=mlbBatting(feed,t,p);return b?Number(b.runs??0):null}
  function getMLBRBI(feed,t,p){const b=mlbBatting(feed,t,p);return b?Number(b.rbi??b.runsBattedIn??0):null}
  function getMLBWalks(feed,t,p){const b=mlbBatting(feed,t,p);return b?Number(b.baseOnBalls??b.walks??0):null}
  function getMLBSB(feed,t,p){const b=mlbBatting(feed,t,p);return b?Number(b.stolenBases??0):null}
  function getMLBTB(feed,t,p){const b=mlbBatting(feed,t,p);if(!b)return null;if(b.totalBases!=null)return Number(b.totalBases);const h=Number(b.hits||0),d=Number(b.doubles||0),tr=Number(b.triples||0),hr=Number(b.homeRuns||0);return Math.max(0,h-d-tr-hr)+d*2+tr*3+hr*4}
  function getMLBKs(feed,t,p){const x=mlbPitching(feed,t,p);return x?Number(x.strikeOuts??x.strikeouts??x.so??0):null}
  function getMLBPitcherOuts(feed,t,p){const x=mlbPitching(feed,t,p);if(!x)return null;if(x.outs!=null)return Number(x.outs);return parseIPToOuts(x.inningsPitched)}
  function getTBFromSummary(summary,t,p){const st=playerStats(summary,t,p);const direct=nStat(st,['TB','totalBases','Total Bases']);if(direct!=null)return direct;const h=getHits(summary,t,p);if(h==null)return null;const hr=getHR(summary,t,p)||0,d=nStat(st,['2B','doubles','Doubles'])||0,tr=nStat(st,['3B','triples','Triples'])||0;return Math.max(0,h-d-tr-hr)+d*2+tr*3+hr*4}
  function baseballStat(kind,summary,feed,t,p){switch(kind){case'hits':return maxKnown(getHits(summary,t,p),getMLBHits(feed,t,p));case'hr':return maxKnown(getHR(summary,t,p),getMLBHR(feed,t,p));case'runs':return maxKnown(getRuns(summary,t,p),getMLBRuns(feed,t,p));case'rbi':return maxKnown(getRBI(summary,t,p),getMLBRBI(feed,t,p));case'walks':return maxKnown(getWalks(summary,t,p),getMLBWalks(feed,t,p));case'sb':return maxKnown(getStolenBases(summary,t,p),getMLBSB(feed,t,p));case'tb':return maxKnown(getTBFromSummary(summary,t,p),getMLBTB(feed,t,p));case'ks':return maxKnown(nStat(playerStats(summary,t,p),['K','SO','Ks','Strikeouts','strikeouts']),getMLBKs(feed,t,p));case'outs':return maxKnown(getPitcherOuts(summary,t,p),getMLBPitcherOuts(feed,t,p));default:return null}}
  async function loadGameData(ticket,leg,event){const summary=event?await fetchSummary(event):null;let mlbFeed=null;if((event?.__sport||C.effectiveLeague(ticket,leg)||'mlb')==='mlb'){try{mlbFeed=await fetchMLBFeed(ticket,leg,event)}catch{}}return{summary,mlbFeed}}
  window.ParlayTrackerSources={resetTrackingCaches,fetchScoreboards,loadGameData,playerStats,nStat,getHits,getHR,getRuns,getRBI,getWalks,getStolenBases,getPoints,getRebounds,getAssists,getBlocks,getThrees,getDoubleCount,getPitcherOuts,getMLBTB,getMLBKs,getTBFromSummary,baseballStat};
})();